// Next.js 설정 파일을 위한 타입을 불러옵니다. 자동 완성을 도와줍니다.
import type { NextConfig } from "next";

// Next.js 프로젝트가 빌드되거나 실행될 때 사용되는 환경 설정 객체입니다.
const nextConfig: NextConfig = {
  /* 
   * 이곳에 필요한 추가 옵션들을 적습니다. 
   * 예시: 이미지 도메인 허용, 리디렉션 처리 등
   */
};

// 이 설정 파일을 바깥으로 내보내어 Next.js 시스템이 읽을 수 있게 합니다.
export default nextConfig;
